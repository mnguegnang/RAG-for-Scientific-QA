#To install local package in the current directory (editable mode) to allow imports from src/ and notebooks/
-e .

# external requirements
# python==3.10.19  # managed by conda, not pip — use: conda create -n qasper-rag python=3.10
numpy==2.2.6
pandas==2.3.3
datasets==2.21.0 #4.5.0 downgrade from 4.5.0 to 2.21.0 to avoid compatibility issues with other packages

sentence-transformers==5.2.3
# faiss: install via conda (supports both CPU and GPU, latest versions only on conda-forge/pytorch channel)
#   CPU (laptop):  conda install -c pytorch faiss-cpu=1.9.0
#   GPU (A100):    conda install -c pytorch -c nvidia faiss-gpu=1.9.0 pytorch-cuda=12.1
# faiss-cpu==1.13.2  ← pip version, kept for reference only
# adapters==1.0.1 # AllenAI adapter library required for allenai/specter2_base (commented out due to transformers dependency issue; use adapters==1.2.0 below)
nltk==3.9.3
rank-bm25==0.2.2
ollama==0.6.1
accelerate>=0.30.0  # Required for device_map="auto" in HuggingFace transformers (GPU backend)
#fastapi==0.133.0
langchain-ollama==1.0.1
langchain-core==1.2.25       # required by langchain-ollama, ragas, and langchain-openai
langchain==1.2.14             # base langchain package
langchain-openai==1.1.10      # ChatOpenAI / OpenAI embeddings (run_rag.py)
langchain-community==0.4.1    # community integrations (loaders, tools)
langchain-text-splitters==1.1.1  # document splitting utilities
langchain-huggingface==1.2.1  # HuggingFaceEmbeddings (GPU evaluation)
einops==0.8.2                 # required by nomic-ai/nomic-embed-text-v1.5
ragas==0.4.3

# ColBERT v2 late-interaction reranker (Santhanam et al., 2022 — NAACL 2022)
# Replaces sentence-transformers CrossEncoder for reranking.
# Provides RAGPretrainedModel.rerank() with MaxSim scoring.
ragatouille>=0.0.8,<0.0.10  # <0.0.10: 0.0.10 switches to PyLate backend; pin Stanford ColBERT backend
                            # Note: ragatouille 0.0.9.x imports langchain.retrievers (removed in langchain 1.0).
                            # src/retrieval/reranker.py injects a compatibility shim at import time.

adapters==1.2.0

#uvicorn[standard]==0.41.0
python-dotenv>=0.5.1
